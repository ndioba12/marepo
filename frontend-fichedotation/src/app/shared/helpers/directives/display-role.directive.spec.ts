import { DisplayRoleDirective } from './display-role.directive';

describe('DisplayRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayRoleDirective();
    expect(directive).toBeTruthy();
  });
});
