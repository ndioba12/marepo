import { StatusDirective } from './status.directive';

describe('EtatDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusDirective();
    expect(directive).toBeTruthy();
  });
});
